## What this changes

Closes #

<!-- Every pull request has to close an issue — CI checks it. Open one first if it doesn't exist
     yet, so the discussion happens before the work. Then one or two sentences here. -->

## Why

<!-- What problem it solves. Skip if it's obvious from the title. -->

## Checks

- [ ] `pnpm lint`, `pnpm typecheck` and `pnpm test` pass
- [ ] Schema change? A migration is committed alongside it (`pnpm db:generate`)
- [ ] Touches crypto, authorization, or the paste read counter? Say so here — those get a closer read

## AI assistance

See [CONTRIBUTING.md](../CONTRIBUTING.md#ai-assistance). Disclosure is not held against you: it tells
the reviewer where to look hardest.

- [ ] No AI tool was used
- [ ] An AI tool was used — which one, and roughly how much of the change:

<!-- e.g. "Claude Code, wrote the first draft of the handler, I rewrote the validation" -->
