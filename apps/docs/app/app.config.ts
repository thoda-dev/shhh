export default defineAppConfig({
  // Same pair as the app (apps/app/app/app.config.ts) so the docs and the instance a reader lands
  // on look like one product. Both names come from the list Docus declares in its nuxt.schema.ts.
  ui: {
    colors: {
      primary: 'amber',
      neutral: 'zinc',
    },
  },
})
